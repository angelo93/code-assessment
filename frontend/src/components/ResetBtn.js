import React from "react";

class ResetBtn extends React.Component {
  handleClick = () => {};

  render() {
    return (
      <button className="reset-btn" onClick={this.handleClick}>
      </button>
    );
  }
}

export default ResetBtn;