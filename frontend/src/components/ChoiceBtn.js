import React from "react";

class ChoiceBtn extends React.Component {
  handleClick = () => {};

  render() {
    return (
      <button className="choice-btn" onClick={this.handleClick}>
      </button>
    );
  }
}

export default ChoiceBtn;
