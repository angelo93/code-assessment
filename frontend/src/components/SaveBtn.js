import React from "react";

class SaveBtn extends React.Component {
  handleClick = () => {};

  render() {
    return (
      <button className="save-btn" onClick={this.handleClick}>
      </button>
    );
  }
}

export default SaveBtn;